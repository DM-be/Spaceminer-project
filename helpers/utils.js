// const utils = {
//     centerGameObjects: (objects) => {
//         objects.forEach(object => object.anchor.setTo(0.5))
//     }
//
// }

// var utils = {
//     centerGameObjects: function (objects) {
//         objects.forEach(function (object) {
//             object.anchor.setTo(0.5);
//         })
//     }
// };

//TODO: for some reason objects for each is not a function??


const utils = {
    centerGameObject: object => object.anchor.setTo(0.5)
};

